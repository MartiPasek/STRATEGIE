# Autentizace & session model appky (2 kanaly identity, demo, magic link)

> oblast: `system-strategie` · úroveň: obor · typ: dokument · verze: V1.0 · rozsah: globální (všichni tenanti)

# Autentizace & session model appky (stav 28.7.2026, doplněno 10.8.2026)  Ucelený obraz, jak funguje přihlášení v STRATEGII (mobil/PWA), po dni ladění s Marti (C23).  ## Dva kanály identity — MOHOU SE ROZEJÍT (past) Nativní Android appka má DVA oddělené kanály: 1. Bearer token (user.carddav_token) — DialPollService (SMS brána / vytáčení). Identita = vlastník tokenu. 2. Webview cookie session (user_id cookie) — JS api() v mobile.html pro VŠECHNA data-volání (Neschopenky, Banka...). V záchytu auth_hdr=False = webview NEposílá Bearer, jede na COOKIE.  KLÍČ: jméno na hlavní obrazovce ("Marti 777220180") pochází z nativního tokenu/device, ale data-volání jedou přes cookie. Když se rozejdou (typicky demo přepsalo cookie), appka se TVÁŘÍ přihlášeně, ale reálně je odhlášená/někdo jiný → 401/403 přesto že "vše vypadá OK". Přesně tohle nás 27.-28.7. dostalo.  RESENI (28.7., commit d1c346396): hlavní obrazovka má proužek KONTROLY SKUTEČNÉ SESSION (endpoint /api/v1/session-mode, čte cookie): zelené nic = reálně přihlášen, oranžové = DEMO, ČERVENÉ = odhlášen/neplatná session i když appka tvrdí opak, s funkčním loginem. Recheck 60s.  ## Přihlášení (magic link e-mail) Guest welcome (když whoami nevrací jméno) → "Přihlásit heslem" / "Přihlásit odkazem / spárovat". SMS kanál vyřazen (coerce sms→email). POST /api/v1/auth/verify-email/request {email, channel} → pošle magic link. Lookup uživatele podle e-mailu (pořadí): ews_display_email → user_contacts (typ email, active) → ews_email (UPN legacy). ANTI-ENUMERATION PAST: když e-mail žádný nesedí (i PŘEKLEP!), vrátí fake token a TIŠE nepošle nic — žádný mail, žádná chyba, jen věčné čekání. (28.7.: Marti napsal "eurosift" místo "eurosoft" → nic nedorazilo.) User1 rozpoznané e-maily: m.pasek@eurosoft.com (display), m.pasek@eurosoft-control.cz (UPN), m.pasek72@gmail.com (kontakt). Zvážit změkčení hlášky pro app-login. Po kliknutí na link → _set_auth_cookies → user_id cookie → přihlášen.  ## AKTUALIZACE 10.8.2026 — platnost session (ZMĚNA proti stavu z 28.7.) Do 10.8. měla cookie user_id natvrdo 30 dní a NIKDE se neobnovovala, takže každý uživatel vypadl přesně 30. den po přihlášení i při denním používání (10.8. doběhla červencová vlna onboardingu = 6 lidí za dopoledne). Nově je platnost settings.session_cookie_max_age_days = 90 dní a middleware v apps/api/main.py ji při každém použití posouvá dopředu (Set-Cookie nejvýš 1x denně, marker cookie stg_sess_ref). Kdo appku používá, nevyprší mu nikdy. Navíc přibylo TICHÉ OBNOVENÍ ze známého zařízení — když user_id cookie chybí, ale telefon pošle platný strategie_device_token, server session obnoví sám bez e-mailu; jen pro role employee a member bez is_admin a bez is_marti_parent. Detail, pasti a postup ověření viz doc-system-strategie-session-trvale-prihlaseni-appky.  ## DEMO režim — teď BEZPEČNÝ (dřív zasekával) "Vyzkoušet ukázku" → GET /api/v1/auth/demo-login → session jako user 104 (login_name=demo). Nutné pro Apple/Google review (Guideline 2.1a). BYL BUG (do 27.7.): demo-login volalo _set_auth_cookies → user_id cookie 30 DNÍ + stg_active handoff → kdokoli klikl (a klikal každý), zůstal tiše zaseknutý jako Demo (104), i rodič; demo se mohlo přes handoff propsat i do Bearer/shared_active. Projev: appka ukazuje "Marti", ale parent-gated (Neschopenky/Banka) = 403 protože cookie=104. FIX (27.7., commit 6a3184a54): demo-login (1) NEpřepíše reálné přihlášení (guard), (2) SESSION-scoped cookies bez max_age, (3) ŽÁDNÝ stg_active handoff. Nový GET /api/v1/auth/exit-demo (smaže auth cookies i staré 30denní) + marker stg_demo=1 + oranžový banner s únikem. POZOR navigace: nativní webview reaguje na fetch, ne vždy na location.href u nových /api URL → tlačítka řešit fetchem + app reload. POZN. 10.8.2026 — demo se z klouzavého prodlužování i z tichého obnovení VYNECHÁVÁ (poznává se podle cookie stg_demo), aby nikdo v demu znovu neuvízl.  ## Práva rodiče — záchranné lano viz doc-system-strategie-prava-rodic-zachranne-lano: is_marti_parent má core-floor {1,6,11} + last-known-good, rodič nikdy nespadne na False kvůli chybě DB. Když je ale session Demo/104, 403 je SPRÁVNĚ (není to blokování rodiče).  ## Dočasná diagnostika (uklidit po 28.7.) fw.diag_nesch (záchyt requestů na neschopenky) + middleware _diag_nesch_capture v main.py + endpoint /app/diag/parent-check. Sloužilo k dopadení cookie=104. Odstranit.

