# EUROSOFT MCP server — Python dependencies for EC-SERVER2 deploy
# Install: pip install -r requirements.txt
#
# Pre-requisite: ODBC Driver 17 for SQL Server (system-level)
#   https://learn.microsoft.com/en-us/sql/connect/odbc/download-odbc-driver-for-sql-server

# MCP SDK
mcp>=1.0.0

# SQL Server connectivity
pyodbc>=5.0.0

# HTTP server
starlette>=0.37.0
uvicorn[standard]>=0.30.0

# Pydantic v2 (transitive via mcp/starlette, pin for safety)
pydantic>=2.0.0

# HTTP client (used by mcp internals)
httpx>=0.27.0
